// Sample array
let numbers = [1, 2, 3, 4, 5, 10];

// Using filter to create a new array with only even numbers
let evenNumbers = numbers.filter((num) => num < 3);
console.log("Even Numbers:", evenNumbers);
