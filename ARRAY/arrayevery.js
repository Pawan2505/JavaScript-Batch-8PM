// Sample array
let numbers = [1, 2,8, 3, 4, 5];

// Using every to check if all numbers in the array are less than 6
let allLessThanSix = numbers.every(e => num < 6);
console.log("All numbers less than 6:", allLessThanSix); // Output: true
